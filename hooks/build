#!/bin/bash
set -eu

echo "=> Docker Hub autobuild hook is intentionally disabled."
echo "=> This repository now uses GitHub Actions as the only supported build/publish path."
echo "=> If you want Docker Hub publishing, configure it as a mirror from .github/workflows/image.yml."
exit 0
